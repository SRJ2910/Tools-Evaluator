[project]
name = "local-tool-evaluator"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "accelerate>=1.13.0",
    "pyinstaller>=6.20.0",
    "safetensors>=0.7.0",
    "sentencepiece>=0.2.1",
    "torch>=2.12.0",
    "transformers>=5.10.2",
]
